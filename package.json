{
  "name": "budgetsheet",
  "version": "1.0.0",
  "description": "BudgetSheet - Google Apps Script Budget Tracker",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && clasp push",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "logs": "clasp logs",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "devDependencies": {
    "chokidar": "^3.6.0",
    "clean-css": "^5.3.3",
    "fast-check": "^3.23.2",
    "html-minifier-terser": "^7.2.0",
    "jest": "^29.7.0",
    "terser": "^5.36.0"
  }
}
